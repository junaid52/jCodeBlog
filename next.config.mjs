/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'vfevzqoujpn1ilo0.public.blob.vercel-storage.com',
        port: '',
      },
    ],
  },
};

export default nextConfig;
